{{! UMD template that can be useful to wrap standalone CommonJS/Node modules}}
(function(root, factory) {
var req = function(id) {return root[id];},
exp = root,
mod = {exports: exp};
{{#if globalAlias}}root['{{globalAlias}}'] = {{else}}{{#if objectToExport}}root['{{objectToExport}}'] = {{/if}}{{/if}}factory({{#if globalDependencies}}{{{globalDependencies}}}, {{/if}}req, exp, mod);
}(this, function({{#if dependencies}}{{dependencies}}, {{/if}}require, exports, module) {
{{{code}}}
{{indent}}return {{#if objectToExport}}{{objectToExport}}{{else}}module.exports{{/if}};
}));
